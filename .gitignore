.anchor
.DS_Store
target
**/*.rs.bk
node_modules
test-ledger
.yarn

# Environment & Credentials
.env
.env.local
.env.*.local
.pem
.key
*.cert
*.crt
credentials/
secrets/

# Opencode & Deployment
opencode.json
opencode.*.json

# Tenity API Certificates
tenity-cert.pem
tenity-key.pem

# Solana Keys
solana-keypairs/
devnet-*.json

# IDE
.vscode/
.idea/
*.swp
*.swo

# SIX FINANCIAL - NEVER COMMIT SECRETS
# Do not add any files from these directories
sixapi/
docs/six-files/
docs/stablehacks2026yoursixdataaccesscredentials/
services/six-oracle/certs/
services/six-oracle/.env

# Certificate and Password Files
*.pem
*.p12
*.key
*password*
*.pfx

# Build output
app/dist/
dist/

# ZK-SNARK - NEVER COMMIT CEREMONY SECRETS
# Powers of Tau ceremony files (contain toxic waste - NEVER commit)
circuits/*.ptau

# Proving keys (contain ceremony secrets - NEVER commit)
**/*.zkey

# ZK build output (generated during compilation)
circuits/*_js/

# ZK public artifacts - must be added explicitly
# These files are safe to commit:
#   app/public/zk/verification_key.json
#   app/public/zk/kyc_verification.wasm
# DO NOT add any .zkey files from app/public/zk/

# ZK source files (safe to commit)
circuits/kyc_verification.circom
circuits/kyc_verification.r1cs
circuits/kyc_verification.sym
